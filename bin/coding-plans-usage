#!/usr/bin/env python3
"""Poll Claude's OAuth usage endpoint into state.json. Thin loader over
coding_plans.providers.claude_usage_api. Run by the systemd timer."""

from __future__ import annotations

import os
import sys
from pathlib import Path

_override = os.environ.get("CODING_PLANS_LIB_DIR")
for _candidate in (
    Path(_override) if _override else None,
    Path.home() / ".local/share/coding-plans-waybar/lib",
    Path(__file__).resolve().parent.parent / "lib",
):
    if _candidate and (_candidate / "coding_plans" / "__init__.py").is_file():
        sys.path.insert(0, str(_candidate))
        break

from coding_plans.providers.claude_usage_api import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
